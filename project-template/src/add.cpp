#include "add.hpp"

// add.cpp
// Implementation of the `add` function declared in add.hpp.
//
// The implementation is intentionally simple and side-effect free.
int add(int a, int b) {
  // Pure function: returns the arithmetic sum of `a` and `b`.
  return a + b;
}
